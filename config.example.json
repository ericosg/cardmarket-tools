{
  "_comment": "Credentials are optional - only required for live API mode (--live flag). Export mode (default) works without credentials.",
  "credentials": {
    "appToken": "your-app-token-here",
    "appSecret": "your-app-secret-here",
    "accessToken": "your-access-token-here",
    "accessTokenSecret": "your-access-token-secret-here"
  },
  "preferences": {
    "country": "DE",
    "currency": "EUR",
    "language": "en",
    "maxResults": 20,
    "defaultSort": "avg"
  },
  "cache": {
    "enabled": true,
    "ttl": 3600
  },
  "export": {
    "enabled": true,
    "autoUpdate": true
  }
}
